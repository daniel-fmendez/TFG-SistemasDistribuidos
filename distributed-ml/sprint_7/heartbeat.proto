syntax = "proto3";

package heartbeat;

service HeartbeatService {
  rpc HeartbeatPing(HeartbeatRquest) returns (HeartbeatResponse);
}

message HeartbeatRquest {
  int64 timestamp = 1;
}

message HeartbeatResponse {
  int64 timestamp = 1;
  string worker_id = 2;
  bool ack = 3;
}